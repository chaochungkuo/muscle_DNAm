# Metadata policy

Only approved, anonymous metadata or public schemas belong here. The datasets used in this research are GSE315831 and GSE121961.

Reviewer-round metadata not approved for public release must be placed under `metadata/private/`, which is ignored by Git, or referenced outside the repository through `config/paths.local.yml`.

All analysis joins must use explicit sample identifiers. Row-order joins are not permitted.
