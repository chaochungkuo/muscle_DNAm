# Python modules

Reusable Python modules for supervised learning, validation, and quantitative
stability metrics belong here. Notebooks and reports should call these modules
rather than passing analysis state through ad hoc pickle files.
